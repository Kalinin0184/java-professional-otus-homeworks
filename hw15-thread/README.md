# hw15-thread

Последовательность чисел: два потока печатают `1..10..1..`, чередуясь через `wait`/`notifyAll`.
Всегда начинает «Поток 1».

```bash
./gradlew :hw15-thread:test
./gradlew :hw15-thread:run
```
